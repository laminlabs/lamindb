# Docs

Use single backticks for inline code, never double backticks.
Sphinx roles like :class:`~lamindb.Feature` stay as they are.

Wrong: Use with :class:`~lamindb.Feature` `maps_to`.

Right: Use with :class:`~lamindb.Feature` `maps_to`.
